import 'package:flutter/material.dart';

class MyConstant {
  final navIcons = [
    Icons.pedal_bike,
    Icons.pages,
    Icons.shopping_cart,
    Icons.document_scanner,
    Icons.people,
  ];

  final scaffoldColor = const Color.fromARGB(255, 9, 14, 30);
  final backPrimaryColor = const Color.fromARGB(255, 9, 97, 168);
  final backSecondaryColor = Colors.blue;
  final backThirdColor = const Color.fromARGB(255, 87, 175, 247);

  final buttonPrimaryColor = const Color.fromARGB(255, 100, 175, 236);
  final buttonSecondaryColor = Colors.white54;

  final textHighLightColor = Colors.blueAccent;
  final textPrimaryColor = Colors.white;
  final textSecondaryColor = Colors.white38;

  final iconPrimaryColor = Colors.white;
  final iconSecondaryColor = Colors.white54;
}
